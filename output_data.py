class OutputData:

	def __init__(self, rows, cols, starting_pos, goal_pos, obstacles, moves):
		self.rows = rows
		self.cols = cols
		self.startingPosition = starting_pos
		self.goalPosition = goal_pos
		self.obstacles = obstacles
		self.moves = moves
